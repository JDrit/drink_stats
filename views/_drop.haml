%li
  =partial(:user, :locals => {:username => drop["username"]})
  dropped a

  =partial(:item, :locals => {:item_name => drop["item_name"]})
  from #{drop["display_name"]} at #{pretty_date(drop["time"])}
